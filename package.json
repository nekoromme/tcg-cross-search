{
  "name": "tcg-cross-search",
  "version": "0.8.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "monitor:server": "node scripts/monitor-server.mjs"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
